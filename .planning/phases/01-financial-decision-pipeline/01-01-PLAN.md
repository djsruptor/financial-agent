---
phase: 01-financial-decision-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - code/main.py
  - code/evaluation/main.py
autonomous: true
requirements: [DATA-01, DATA-03, CASH-01, CASH-03, CASH-04, FORE-01, FORE-02, FORE-03, EVAL-02]
user_setup: []
must_haves:
  truths:
    - "A structured request produces checked baseline capacity without replaying settled history."
    - "Every schedule is checked over all 90 days, including dates before its first payment."
    - "Unresolved evidence and an infeasible baseline are distinct outcomes."
  artifacts:
    - path: code/main.py
      provides: "Validated financial tools and baseline CLI"
    - path: code/evaluation/main.py
      provides: "Independent numerical regression checks"
  key_links:
    - from: code/evaluation/main.py
      to: code/main.py
      via: "Calls the same financial functions used by the agent"
---

<objective>
Implement the deterministic financial tools the request orchestrator will use. Deliver one structured-request path plus numerical checks in the existing entry points. The production model-driven loop is integrated in Plan 01-02; these pure tools never delegate money calculations to the model.
</objective>

<execution_context>
@/Users/Area92/.codex/get-shit-done/workflows/execute-plan.md
@/Users/Area92/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@problem_statement.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md
@code/main.py
@code/evaluation/main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Load a request into validated cash state</name>
  <files>code/main.py, code/evaluation/main.py</files>
  <read_first>AGENTS.md, problem_statement.md, .planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, dataset/financial_events.csv, dataset/financial_profiles.csv</read_first>
  <action>
Implement load_dataset, build_request_context and reconstruct_cash_state with stdlib csv/pathlib/Decimal/dates. Expose python3 code/main.py --request-id ID --baseline --structured-only for tool development. Load evaluation requests normally; the evaluator passes stripped sample input fields directly to the same functions. Enforce immutable inputs, authorized identifiers, finite money, knowledge-time filtering, current snapshot semantics, pending-debit reserves and real-versus-duplicate lifecycle rules from CONTEXT. Preserve relevant unresolved evidence IDs; return analysis_error when required evidence prevents a forecast, never a fabricated zero. Use supplied settlement FX only. Add --checks input in the existing evaluator with direct assertions for settled history, pending credits/debits, cancelled/unrealized events, distinct linked refund, duplicate obligation, invalid/nonfinite money and missing FX. Include the independent FX numerical oracle from CONTEXT.
  </action>
  <verify><automated>python3 code/evaluation/main.py --checks input</automated></verify>
  <acceptance_criteria>Input checks exit zero; USD100 at supplied 0.9 consumes EUR90; settled history leaves the profile snapshot unchanged; missing amounts remain unresolved; labels cannot enter predictor context.</acceptance_criteria>
  <done>A validated request snapshot and explicit unresolved-evidence list feed deterministic tools.</done>
</task>

<task type="auto">
  <name>Task 2: Implement supported recurrence and whole-horizon capacity</name>
  <files>code/main.py, code/evaluation/main.py</files>
  <read_first>.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, code/main.py, code/evaluation/main.py</read_first>
  <action>
Implement project_flows, check_schedule and forecast_baseline using the concrete date/recurrence/essential policies in CONTEXT. Use ordinary dicts/lists with source IDs, dates, Decimal amounts and direction; no policy classes. check_schedule returns safe, balance_path and first_breach. forecast_baseline returns baseline_feasible, safe amount, earliest date and normalized context; baseline failure has safe0/dateNone, while unresolved inputs are analysis_error. All candidate dates replay the whole horizon with the same checker. Apply known recurrence exceptions before projecting; deduplicate explicit occurrences and reserve only residual category budgets. Add --checks forecast with every synthetic numerical oracle in CONTEXT, including pre-payment breach, exact floor, D+89 boundary, same-day order, monthly clamp/restoration, essential cadence and recurrence replacement. Include an irregular-essential budget case with block totals60/90/75 and explicit block debit30: total reserve90, not120. Preserve original source IDs for Phase 2 adjustments.
  </action>
  <verify><automated>python3 code/evaluation/main.py --checks forecast</automated></verify>
  <acceptance_criteria>Later-pressure case yields safe300/date2026-01-21; pre-payment breach yields infeasible/safe0/dateNone; monthly replacement totals200; no schedule can skip an earlier breach; unsupported future credits never appear.</acceptance_criteria>
  <done>Pure financial tools produce independently checked amounts/dates and expose the reusable schedule checker.</done>
</task>

<task type="auto">
  <name>Task 3: Close the structured slice with a public numerical oracle</name>
  <files>code/main.py, code/evaluation/main.py</files>
  <read_first>dataset/sample_requests.csv, .planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, code/main.py, code/evaluation/main.py</read_first>
  <action>
Add --checks core to run input plus forecast checks and --samples request_01 --structured-only to evaluate the stripped public request through the same functions. Assert safe25256/date2024-03-03 in evaluator only. Verify a supplied foreign-currency event's settlement/rate/Decimal conversion without copying expected labels into production. If a policy disagrees with public evidence, diagnose it, document the general rule correction in the execution summary and add its independent regression before declaring completion. Do not weaken expected values, implement per-ID exceptions or claim passing without a run.
  </action>
  <verify><automated>python3 code/evaluation/main.py --checks core &amp;&amp; python3 code/evaluation/main.py --samples request_01 --structured-only</automated></verify>
  <acceptance_criteria>All core checks and request_01 exact capacity/date pass; output includes baseline feasibility; no recommendation selection or final CSV is generated.</acceptance_criteria>
  <done>The tools have a working structured slice and are ready for orchestrator-controlled evidence resolution.</done>
</task>
</tasks>

<threat_model>
Untrusted CSV fields and source references cross the loader boundary. Reject malformed/nonfinite money, unauthorized joins, and missing FX; never use labels in prediction. check_schedule is the sole safety authority and checks the full horizon. Do not write input files or final output in this plan.
</threat_model>

<verification>
Run both commands in Task 3; inspect that the evaluator invokes production functions and expected values remain evaluator-only. Record numerical results and any justified policy correction in 01-01-SUMMARY.md.
</verification>

<success_criteria>
All three tasks pass. The observable truths above hold; unresolved evidence and baseline infeasibility cannot be confused. No Phase 2 selection logic or extra infrastructure is added.
</success_criteria>

<output>
After execution create .planning/phases/01-financial-decision-pipeline/01-01-SUMMARY.md with actual checks and decisions.
</output>
