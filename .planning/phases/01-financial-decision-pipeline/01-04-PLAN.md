---
phase: 01-financial-decision-pipeline
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - code/main.py
  - code/evaluation/main.py
  - code/README.md
autonomous: true
requirements: [CASH-01, CASH-03, CASH-04, FORE-01, FORE-02, FORE-03, EVAL-02, AGENT-01]
user_setup:
  - "Live acceptance requires a working OPENAI_API_KEY and network access; without them the run must remain credential_blocked."
must_haves:
  truths:
    - "request_02 and request_03 match their published baseline amount/date oracles through the production run path without request-ID branches."
    - "The correction is a general cash-flow or evidence rule supported by an independent regression, not a label-specific offset."
    - "The evaluator distinguishes deterministic correctness from live-provider availability."
  artifacts:
    - path: code/main.py
      provides: "Corrected general forecast/evidence behavior"
    - path: code/evaluation/main.py
      provides: "Independent request_02/request_03 regressions and explicit live gate"
    - path: code/README.md
      provides: "Clear offline versus live acceptance commands"
  key_links:
    - from: code/evaluation/main.py
      to: code/main.py
      via: "All three sample fixtures call run_request and deterministic forecast tools"
---

<objective>
Close the remaining Phase 1 verification gaps after 01-03: diagnose and fix the request_02/request_03 baseline discrepancies using general rules, then keep live credential availability explicit.
</objective>

<execution_context>
@/Users/Area92/.codex/get-shit-done/workflows/execute-plan.md
@/Users/Area92/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@problem_statement.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md
@.planning/phases/01-financial-decision-pipeline/01-VERIFICATION.md
@.planning/phases/01-financial-decision-pipeline/01-03-SUMMARY.md
@code/main.py
@code/evaluation/main.py
@dataset/sample_requests.csv
</context>

<tasks>
<task type="auto">
  <name>Task 1: Diagnose the two remaining sample discrepancies</name>
  <files>code/main.py, code/evaluation/main.py</files>
  <read_first>.planning/phases/01-financial-decision-pipeline/01-VERIFICATION.md, .planning/phases/01-financial-decision-pipeline/01-03-SUMMARY.md, .planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, code/main.py, dataset/financial_events.csv, dataset/financial_profiles.csv, dataset/messages.csv, dataset/images.csv</read_first>
  <action>Trace request_02 and request_03 from profile snapshot through normalized events, evidence facts, projected flows, every balance-path movement and candidate-date check. Print a sanitized ledger of dates, source IDs, direction, Decimal amount and running balance; do not print PII or sample answer columns. Compare each discrepancy to the concrete policies in CONTEXT: pending-debit reservation, settled-history snapshot, salary amendment effective date, linked image net amount, supported recurrence, protected/irregular essential residuals, same-day ordering and D through D+89 boundaries. Identify one general rule defect per discrepancy before editing. Do not add request-ID conditionals or numeric offsets.
  </action>
  <verify><automated>python3 code/evaluation/main.py --samples request_02,request_03 --structured-only</automated></verify>
  <acceptance_criteria>A reproducible ledger identifies the exact source flow(s) explaining 18671640.83 versus 17229139.2 and 866764.95 versus 873000; no source labels or hidden data are used; each proposed fix is stated as a general rule.</acceptance_criteria>
  <done>Root causes are documented before changes are made.</done>
</task>

<task type="auto">
  <name>Task 2: Correct general rules and lock exact regressions</name>
  <files>code/main.py, code/evaluation/main.py</files>
  <read_first>.planning/phases/01-financial-decision-pipeline/01-04-PLAN.md, .planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, code/main.py, code/evaluation/main.py</read_first>
  <action>Apply only the general rule corrections found in Task 1. Extend independent synthetic regressions for the affected behavior (for example effective-date amendment, explicit-versus-synthetic recurrence, net-image amount, reserve residual or FX direction) so the correction is not encoded as a sample exception. Ensure sample_check sends only stripped request fields plus source-shaped evidence through run_request. Assert exact Decimal amount/date pairs for request_01=25256/2024-03-03, request_02=17229139.2/2025-09-15 and request_03=873000/2019-11-15. Keep production code unable to read sample answer fields or branch on request IDs.
  </action>
  <verify><automated>python3 code/evaluation/main.py --checks phase1 &amp;&amp; python3 code/evaluation/main.py --samples request_01,request_02,request_03 --structured-only</automated></verify>
  <acceptance_criteria>All three exact pairs pass; prior financial, essentials, image and agent regressions remain green; a code search finds no request-ID branch in production; the new regression fails if the corrected general rule is reverted.</acceptance_criteria>
  <done>Deterministic Phase 1 correctness is closed independently of provider access.</done>
</task>

<task type="auto">
  <name>Task 3: Recheck live acceptance without hiding the credential gate</name>
  <files>code/evaluation/main.py, code/README.md</files>
  <read_first>code/README.md, code/evaluation/main.py, .planning/phases/01-financial-decision-pipeline/01-CONTEXT.md</read_first>
  <action>Run the documented live command only when OPENAI_API_KEY is present. Preserve explicit credential_blocked behavior, measured usage and non-zero exit when the provider cannot complete an action; never substitute scripted responses for live acceptance. When credentials are available, require the same three exact pairs, source-grounded message/image facts, bounded model-selected tool sequence and actual provider/model/token totals. Update README diagnostics only to clarify prerequisite and result states; do not add a second provider or routing layer.
  </action>
  <verify><automated>python3 code/evaluation/main.py --samples request_01,request_02,request_03 --live</automated></verify>
  <acceptance_criteria>Without credentials the command reports credential_blocked and remains incomplete; with credentials it passes only after three exact pairs and measured usage pass; no fabricated usage is emitted.</acceptance_criteria>
  <done>Phase status can distinguish closed deterministic gaps from an external live-provider blocker.</done>
</task>
</tasks>

<threat_model>
Sample labels and model output remain untrusted. Host-owned Decimal forecast and evidence validation are authoritative. A sanitized ledger must not expose PII. Live credentials never enter code, logs or artifacts; credential failure cannot be converted into financial infeasibility or a passing acceptance result.
</threat_model>

<verification>
Run all three task commands and re-run the phase verifier. If live credentials remain unavailable, record only credential_blocked after deterministic checks pass; do not mark the phase fully complete.
</verification>

<success_criteria>
The two deterministic sample mismatches are fixed by general rules with independent regressions. Live acceptance is either fully measured and passing or explicitly blocked by credentials, with no weakened oracle or new infrastructure.
</success_criteria>

<output>
After execution create 01-04-SUMMARY.md and update 01-VERIFICATION.md with deterministic and live results.
</output>
