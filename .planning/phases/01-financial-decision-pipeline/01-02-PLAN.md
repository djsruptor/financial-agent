---
phase: 01-financial-decision-pipeline
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - code/main.py
  - code/evaluation/main.py
  - code/requirements.txt
  - code/README.md
autonomous: true
requirements: [AGENT-01, EVID-01, EVID-02, EVID-04, DATA-01, CASH-04, FORE-01, FORE-02, FORE-03, EVAL-02]
user_setup:
  - "For live acceptance, provide the selected provider credential through its documented environment variable if unavailable; never put credentials in files or logs."
must_haves:
  truths:
    - "A bounded model-driven orchestrator selects tools based on observations and completes a checked financial baseline."
    - "User-level messages and linked images update only validated authorized facts."
    - "The model cannot bypass financial checks, finish with stale evidence, or invent output amounts."
    - "Three named public requests match exact baseline oracles and live calls report actual usage."
  artifacts:
    - path: code/main.py
      provides: "Request orchestrator, financial tools, evidence validation and usage counters"
    - path: code/evaluation/main.py
      provides: "Offline agent replays, evidence checks and live public-sample acceptance"
    - path: code/requirements.txt
      provides: "One pinned model SDK"
    - path: code/README.md
      provides: "Concrete model, environment setup and offline/live commands"
  key_links:
    - from: code/main.py
      to: code/main.py
      via: "Allowlisted tool dispatch to validated facts and the shared forecast_baseline/check_schedule"
    - from: code/evaluation/main.py
      to: code/main.py
      via: "Scripted and live clients exercise the same run_request loop"
---

<objective>
Make the agentic workflow core: the model investigates authorized evidence, chooses allowlisted tools based on results, and requests deterministic financial verification. Complete the Phase 1 path with bounded execution and actual usage. No multi-agent framework or separate extraction service.
</objective>

<execution_context>
@/Users/Area92/.codex/get-shit-done/workflows/execute-plan.md
@/Users/Area92/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@problem_statement.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md
@.planning/phases/01-financial-decision-pipeline/01-01-SUMMARY.md
@code/main.py
@code/evaluation/main.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build the bounded orchestrator around the financial tools</name>
  <files>code/main.py, code/evaluation/main.py, code/requirements.txt, code/README.md</files>
  <read_first>.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, code/main.py, code/evaluation/main.py</read_first>
  <action>
Implement run_request and a literal allowlisted dispatcher for inspect_records, resolve_evidence, forecast_baseline and finish as defined in CONTEXT. The model chooses next tools from observations; host code enforces schemas, per-request scope, unresolved evidence, revisions, budgets and termination. A forecast or finish attempt before required evidence resolution returns a structured error; finish returns only host-owned result fields. Register resolve_evidence initially with an explicit unresolved response until Task 2 fills it. Use a simple injected callable for a scripted test client, not a provider framework. Select one accessible vision/tool-capable provider/model, verify official API docs during implementation, pin only its SDK, and document actual model ID, environment variable and commands. Build all offline behavior even if credentials are absent; live acceptance remains pending. Count all provider attempts and response-reported usage, including orchestration/extraction and retries. Enforce the 12-turn/24-attempt/60-second limits and at most two interpretations per evidence item. Add --checks agent for branching, attempted early finish, unknown tools, cross-user IDs, stale forecast, malformed outputs, repeated failure and budget exhaustion. No paid calls in offline tests.
  </action>
  <verify><automated>python3 code/evaluation/main.py --checks agent</automated></verify>
  <acceptance_criteria>Scripted transcripts follow different tool paths for complete versus unresolved contexts; no direct model amount is accepted; each failure terminates or returns a bounded validation observation; actual response usage is accumulated without fabricated values.</acceptance_criteria>
  <done>The production agent loop owns workflow decisions while deterministic tools own financial authority.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve authorized evidence and invalidate stale forecasts</name>
  <files>code/main.py, code/evaluation/main.py</files>
  <read_first>.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, dataset/messages.csv, dataset/images.csv, code/main.py</read_first>
  <action>
Implement resolve_evidence with the same selected client/model. Return source-scoped proposed facts and host-validate finite money, dates, effect, currency, event relationship when present, and user/request scope. A missing event ID is legitimate: resolve user-level salary/commitment changes to a supported scope, never fabricate an event link. Apply conflict precedence and idempotence, increment evidence revision and invalidate prior forecasts after accepted changes. Read linked PNGs only through the supplied image index, reject paths outside the media directory, and extract the requested net/cash field rather than a prominent gross number. Keep required ambiguous facts unresolved; a model assertion that an item is irrelevant cannot silently dismiss a blank required amount or material amendment. Injected instructions remain data and cannot authorize tools or policy changes. Add --checks evidence with synthetic user-level salary100-to120, duplicate amendment, wrong-user target, future-message exclusion, malformed/nonfinite amounts, missing/ambiguous image, gross-versus-net and instruction injection. Include an orchestration replay: resolve evidence, forecast, amend evidence, reject stale finish, recompute, finish.
  </action>
  <verify><automated>python3 code/evaluation/main.py --checks evidence</automated></verify>
  <acceptance_criteria>Unlinked salary amendment changes only future matching income to120 once; injected policy changes fail; blank/ambiguous image never becomes0; stale forecasts cannot finish; usage includes evidence responses.</acceptance_criteria>
  <done>Messages/images are validated agent tool observations that safely update the deterministic context.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the agentic path against exact sample results</name>
  <files>code/main.py, code/evaluation/main.py, code/README.md</files>
  <read_first>.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, dataset/sample_requests.csv, code/main.py, code/evaluation/main.py</read_first>
  <action>
Add --checks phase1 to combine core/agent/evidence checks, and --samples request_01,request_02,request_03 --live to run stripped sample inputs through the real run_request loop. Assert the exact three amount/date pairs in CONTEXT and source facts message_01 salary42750000/effective2025-08-15 plus image_01/event_253 net4365000. Check at least one supplied foreign-currency event and its actual settlement-rate arithmetic. Emit concise evaluator diagnostics with request/source IDs, expected versus actual numbers, tool sequence and aggregate usage; omit secrets, full documents and private reasoning. Correct general rule defects with regressions; do not relax labels or hardcode requests. Live credentials or model failures are explicit blockers, never skipped passing checks. Document offline/live commands and report actual model/calls/tokens. Expose the checked result and shared schedule checker for Phase 2; do not implement payment selection, final CSV, all-25 scoring or packaging.
  </action>
  <verify><automated>python3 code/evaluation/main.py --checks phase1 &amp;&amp; python3 code/evaluation/main.py --samples request_01,request_02,request_03 --live</automated></verify>
  <acceptance_criteria>Offline suite passes; all three live sample amount/date pairs match; message/image facts match source evidence; tool observations show model-selected actions; usage is measured; missing credentials or a mismatch leaves acceptance incomplete.</acceptance_criteria>
  <done>The real agent resolves evidence and returns numerically verified baselines; Phase 2 can extend the same loop with deterministic payment tools.</done>
</task>
</tasks>

<threat_model>
Messages, images and model-generated tool arguments are untrusted. Host allowlists and scope validation prevent cross-user access and arbitrary execution. Independent fact validation, revision checks and mandatory whole-horizon forecasting prevent invented/stale financial output. Time/turn/attempt limits bound loops; unavailable evidence returns analysis_error rather than a financial rejection. Secrets stay in environment variables; only concise sanitized observations and response usage are reported.
</threat_model>

<verification>
Run Task 3's combined offline/live command. Inspect at least one live tool sequence and source-grounded extraction. Verify no sample output field crosses the model boundary and all money comes from deterministic tools. Any unresolved live prerequisite, numerical mismatch or budget failure blocks phase completion.
</verification>

<success_criteria>
All three tasks pass, including actual live model evidence. Agentic orchestration is observable and bounded, all financial invariants remain enforced by code, and the Phase 1 result is ready for the existing Phase 2 scope.
</success_criteria>

<output>
After execution create .planning/phases/01-financial-decision-pipeline/01-02-SUMMARY.md with exact results, usage and remaining blockers. Do not mark completion from offline simulations alone.
</output>
