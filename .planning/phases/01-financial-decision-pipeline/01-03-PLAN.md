---
phase: 01-financial-decision-pipeline
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - code/main.py
  - code/evaluation/main.py
  - .planning/ROADMAP.md
autonomous: true
requirements: [CASH-01, CASH-04, EVID-01, EVID-02, EVID-04, EVAL-02]
user_setup:
  - "Live acceptance still requires a working OPENAI_API_KEY; an unavailable provider remains an explicit blocker."
must_haves:
  truths:
    - "Protected and irregular essential spending is included conservatively without double counting."
    - "Linked image amounts are source-grounded; arbitrary model numbers are rejected."
    - "All three public sample baselines are asserted through the production run path offline, while live provider status remains explicit."
    - "Message conflict precedence and user-level amendments are deterministic and tested."
  artifacts:
    - path: code/main.py
      provides: "Complete protected-essential, conflict and image-source validation"
    - path: code/evaluation/main.py
      provides: "Gap regressions and exact three-sample oracle checks"
  key_links:
    - from: code/main.py
      to: code/evaluation/main.py
      via: "Production run path is exercised by offline source-shaped sample fixtures"
---

<objective>
Close the three blockers reported by 01-VERIFICATION.md without expanding the product: source-grounded linked-image extraction, protected/irregular essential reserves and conflict precedence, and exact offline oracle coverage for all three public samples.
</objective>

<execution_context>
@/Users/Area92/.codex/get-shit-done/workflows/execute-plan.md
@/Users/Area92/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@problem_statement.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md
@.planning/phases/01-financial-decision-pipeline/01-VERIFICATION.md
@.planning/phases/01-financial-decision-pipeline/01-02-SUMMARY.md
@code/main.py
@code/evaluation/main.py
@dataset/messages.csv
@dataset/images.csv
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement protected and irregular essential reserves</name>
  <files>code/main.py, code/evaluation/main.py</files>
  <read_first>.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, .planning/phases/01-financial-decision-pipeline/01-VERIFICATION.md, code/main.py, dataset/financial_profiles.csv</read_first>
  <action>Read expense_categories_to_protect and the user's permitted categories into the request context. Implement the named recurrence policy and essential residual budget: three supported occurrences are required, periodic variable essentials use the maximum of the last three, unsupported cadence uses the maximum total across the last three complete 30-day windows as a daily reserve, and explicit same-category debits reduce only the residual budget. Keep fixed/protected events intact, preserve source IDs, and never reserve an obligation twice. Implement conflict precedence (explicit change, newer same-source, settled, safer supported interpretation) and make amendments/cancellations invalidate the forecast revision. Add independent synthetic checks for protected categories, block totals60/90/75 with explicit debit30 yielding total90, duplicate obligations, and conflicting amendment/cancellation.</action>
  <verify><automated>python3 code/evaluation/main.py --checks essentials</automated></verify>
  <acceptance_criteria>Protected categories are present in the forecast; irregular essential reserve follows the 60/90/75 oracle; explicit debits are not duplicated; conflicts resolve in the documented order; existing phase1 checks remain green.</acceptance_criteria>
  <done>CASH-04 and the missing conflict behavior are implemented and independently exercised.</done>
</task>

<task type="auto">
  <name>Task 2: Make linked image evidence source-grounded</name>
  <files>code/main.py, code/evaluation/main.py</files>
  <read_first>.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, .planning/phases/01-financial-decision-pipeline/01-VERIFICATION.md, code/main.py, dataset/images.csv, dataset/media/images/image_01.png</read_first>
  <action>Keep image resolution restricted to the supplied image index and media directory. Add a narrow extraction adapter that validates the requested financial field against the image's extracted content before _apply_evidence mutates state. Require the linked event and expected field/currency/date context; reject arbitrary finite values, wrong-field gross values, multiple candidates, missing files and non-finite values. Use a deterministic OCR/parser fixture for offline checks so image_01/event_253 accepts the source net IDR4365000 and rejects IDR1 and the gross amount; use the selected model only for live interpretation. Do not add OCR services, caches or provider routing. Keep image text untrusted and preserve usage counters for live extraction.</action>
  <verify><automated>python3 code/evaluation/main.py --checks image-evidence</automated></verify>
  <acceptance_criteria>image_01/event_253 accepts only the source-grounded net amount; IDR1 and gross candidates are rejected; blank/ambiguous evidence remains analysis_error and never becomes zero; all prior agent/evidence checks pass.</acceptance_criteria>
  <done>EVID-02 and EVID-04 no longer trust an arbitrary model-provided image amount.</done>
</task>

<task type="auto">
  <name>Task 3: Add all three exact offline sample oracles and correct MVP metadata</name>
  <files>code/evaluation/main.py, .planning/ROADMAP.md</files>
  <read_first>.planning/phases/01-financial-decision-pipeline/01-CONTEXT.md, .planning/phases/01-financial-decision-pipeline/01-VERIFICATION.md, dataset/sample_requests.csv, code/evaluation/main.py</read_first>
  <action>Extend the evaluator's source-shaped scripted client so request_01, request_02 and request_03 all run through run_request and assert exact amount/date pairs: 25256/2024-03-03, 17229139.2/2025-09-15, and 873000/2019-11-15. The fixtures must supply general evidence facts (including message_01 salary IDR42750000 effective2025-08-15 and image_01/event_253 net IDR4365000), never branch on request IDs inside production code or read sample answer columns. Diagnose and fix general forecast rules when an oracle fails. Keep --live as a separate explicit provider gate: report credential_blocked or mismatch as incomplete, never pass offline in its place. Change Phase 1's roadmap goal to a valid user story beginning 'As a user...'; keep the same lean scope. Add --checks gaps to run essentials, image-evidence and all three sample checks.</action>
  <verify><automated>python3 code/evaluation/main.py --checks gaps &amp;&amp; python3 code/evaluation/main.py --samples request_01,request_02,request_03 --structured-only</automated></verify>
  <acceptance_criteria>All three Decimal amount/date pairs pass through the production request path offline; diagnostics identify tool sequence and evidence sources; no production sample-label access exists; roadmap Phase 1 goal is a valid user story; live status remains explicit.</acceptance_criteria>
  <done>The offline suite cannot pass while request_02/request_03 or source-grounded image handling is wrong.</done>
</task>
</tasks>

<threat_model>
Profile categories, messages, images and scripted/model facts are untrusted inputs. Host validation owns source linkage, conflict precedence and all money. Deterministic reserve and schedule checks remain the sole financial authority. Sample fixtures are evaluator-only and stripped before production prediction contexts. No input files are modified.
</threat_model>

<verification>
Run all three task commands plus `python3 code/evaluation/main.py --checks phase1`. Inspect that image acceptance uses source-grounded content and that all three sample assertions execute via run_request. Re-run the verifier; live credentials or residual numerical mismatch keep the phase in Needs Review.
</verification>

<success_criteria>
All offline gap checks pass, exact sample oracles pass, and the verifier reports no implementation gaps other than an explicitly credential-blocked live provider run. No new service, framework, cache or product feature is introduced.
</success_criteria>

<output>
After execution create .planning/phases/01-financial-decision-pipeline/01-03-SUMMARY.md and re-run verification to update 01-VERIFICATION.md.
</output>
